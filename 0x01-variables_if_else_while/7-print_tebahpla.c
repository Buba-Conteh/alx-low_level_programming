#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main- entry point
*
*
* Return: always 0
*/
int main(void)
{
	/*main Write a program that prints the */
	char ch;

	for (ch = 'z' ; ch >= 'a' ; ch--)
	putchar (ch);

	putchar ('\n');

	return (0);
}
